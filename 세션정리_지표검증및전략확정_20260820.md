# 세션 정리 — 지표 정밀검증 및 전략 확정 (2026-08-19 ~ 20)

## 1. 키움 REST API 직접 연동 시도 (get_data.py)

- `get_data.py`에 키움 REST API(`ka10081`, 주식일봉차트조회요청) 호출 기능을 추가했다.
  토큰 발급은 `get_kiwoom_ranking.py`의 `load_app_credentials()`/`issue_access_token()`을
  재사용하고, `cont-yn`/`next-key` 페이지네이션으로 5년치 일봉을 모은다.
- 목적: FinanceDataReader와 영웅문(키움) 간 데이터 벤더 차이 때문에 뒀던 보정값
  (`FACTOR=1.1`, 아래 2번 참고)을 근본적으로 없애는 것.
- **현재 상태: 키움 API가 막혀 있다.** 토큰 발급 시 `return_code: 3`,
  `"인증에 실패했습니다[8050:지정단말기 인증에 실패했습니다]"` 에러가 계속 발생한다.
  IP 제한을 해제해봤지만 동일 에러가 재현됐다 — IP 화이트리스트가 아니라 "지정단말기
  인증"이라는 별도 항목(키움 개발자센터의 단말기 등록/보안 옵션)이 원인으로 보이며,
  아직 해결되지 않았다.
- 그래서 이번 세션의 모든 데이터 수집은 `python get_data.py --source fdr ...`로,
  즉 기존 FinanceDataReader 경로로 진행했다. `--source kiwoom` 경로는 코드상으로는
  완성돼 있으니 키움 쪽 인증 문제가 풀리면 바로 시도해보면 된다.

## 2. 지표 공식 정밀검증 및 수정

### 2-1. `fibo_indicator.py` — MA5/MA8/MA13을 EMA → 단순이동평균(SMA)으로 변경

- 기존에는 `add_ema()`가 `Close.ewm(span=n, adjust=False)`로 지수이동평균(EMA)을
  계산했다. 이번에 요청받은 정확한 공식은 "단순이평(SMA), 종가기준"이라, 함수를
  `add_ma()`로 이름을 바꾸고 `Close.rolling(window=n).mean()`으로 교체했다.
- 피보4 = (MA5+MA8)/2, 피보5 = (MA8+MA13)/2 공식 자체는 그대로다 — 안에 들어가는
  MA의 종류만 EMA에서 SMA로 바뀌었다.
- 호출부인 `backtest.py`의 `fi.add_ema(df)`도 `fi.add_ma(df)`로 함께 수정했다
  (이 프로젝트에서 `add_ema`를 부르는 곳은 여기 한 곳뿐이었다).
- **주의**: 이 변경으로 삼성전자 단일종목 백테스트(`backtest.py`)의 골든/데드크로스
  타이밍과 매매 결과도 함께 바뀐다 — MA 계산 방식이 바뀌었으니 당연한 결과다.

### 2-2. `ichimoku_custom_indicator.py` — 엔상/엔하 FACTOR 1.1 → 1, 시프트 제거

- 이전 세션에서 영웅문 값과 맞춰보려고 역산해 넣었던 보정계수 `FACTOR=1.1`을,
  이번에 요청받은 원본 공식 그대로 `FACTOR=1`로 되돌렸다.
- 라인 공식(`LINE_SPECS`: 2선=AVG(ATR,10)×Factor, 3선=AVG(ATR,20)×Factor×2,
  4선=×3, 5선=×4)은 이미 정확히 일치해서 손대지 않았다.
- 항상 0이던 시프트 파라미터 `AA`(및 `.shift(AA)` 호출)를 제거했다 — 요청받은
  공식에 시프트 항목이 없어서, 있으나 마나 하던 코드를 걷어냈다.
- 참고: 엔상/엔하(ATR 인벨로프)는 이번에 확정한 백테스트 전략(아래 3번)에는
  쓰이지 않는다. 차트 시각화(`data/samsung_ichimoku_custom.png`)용 참고 지표다.

### 2-3. `ichimoku_custom_indicator.py` — 선1/선2(일목 선행스팬, 현재위치) 신규 추가

새 함수 `add_senkou_spans()`:

- 선1 = (HH10 + LL10 + HH25 + LL25) / 4
- 선2 = (HH50 + LL50) / 2
  (HHn/LLn = 최근 n일 고가 최댓값 / 저가 최솟값)
- 미래로 미리 그리는 시프트 없이(현재 위치 그대로) 계산한다.
- 양운/음운 판정(`구름상태` 컬럼): 선1 > 선2 → 양운, 선1 < 선2 → 음운.
  백테스트 전략에서는 문자열 컬럼 대신 `선1`/`선2` 값을 직접 비교해서 쓴다
  (부동소수점 동률 등 문자열 파싱 이슈를 피하기 위함).

## 3. 전략 확정 — "양운 + 피보 크로스"

`backtest_multi.py`에 구현. `backtest.py`가 ADX를 매수 필터로 쓰던 자리에
양운 여부를 넣은 구조다.

- **매수**: 피보 골든크로스(피보 구름 중심이 역피보 구름 중심을 아래→위로 돌파)가
  뜬 날, 그날 선1 > 선2(양운)이면 **다음 거래일 시가**에 매수. 양운이 아니면
  골든크로스가 떠도 매수하지 않는다.
- **매도**: 피보 데드크로스가 뜨면 양운/음운과 무관하게 무조건 **다음 거래일 시가**에
  매도.
- 종목당 동시에 1포지션만 보유(이미 보유 중이면 골든크로스 무시, 미보유 중이면
  데드크로스 무시).
- 매매 수수료(0.015%)/거래세(0.18%)는 `backtest.py`의 `BUY_FEE_RATE`/`SELL_FEE_RATE`를
  그대로 재사용.

## 4. 다종목 백테스트 결과

### 4-1. 시가총액 상위 + 업종 다양화 8종목 (`--group stocks`)

| 종목코드 | 종목명 | 매매건수 | 승률(%) | 손익비 | 총수익률(%) | MDD(%) |
|---|---|---|---|---|---|---|
| 005930 | 삼성전자 | 11 | 36.36 | 8.17 | 138.97 | -14.98 |
| 000660 | SK하이닉스 | 16 | 31.25 | 5.80 | 125.80 | -30.14 |
| 035420 | NAVER | 6 | 50.00 | 1.11 | 0.39 | -12.17 |
| 035720 | 카카오 | 8 | 12.50 | 0.75 | -41.53 | -39.27 |
| 373220 | LG에너지솔루션 | 8 | 25.00 | 0.98 | -34.33 | -42.69 |
| 005380 | 현대차 | 13 | 30.77 | 5.59 | 48.41 | -12.88 |
| 105560 | KB금융 | 20 | 30.00 | 0.51 | -53.24 | -49.10 |
| 207940 | 삼성바이오로직스 | 11 | 18.18 | 3.52 | -9.55 | -13.53 |
| **전체** | **전체 합산(균등비중)** | **93** | **29.03** | **3.33** | **21.86** | **-14.21** |

파일: `data/backtest_results_multi.csv`, `data/backtest_trades_multi.csv`

### 4-2. 실제 보유종목 9종목 (`--group holdings`)

| 종목코드 | 종목명 | 매매건수 | 승률(%) | 손익비 | 총수익률(%) | MDD(%) |
|---|---|---|---|---|---|---|
| 005930 | 삼성전자 | 11 | 36.36 | 8.17 | 138.97 | -14.98 |
| 000660 | SK하이닉스 | 16 | 31.25 | 5.80 | 125.80 | -30.14 |
| 278470 | 에이피알 | 10 | 30.00 | 7.69 | 83.27 | -18.72 |
| 010170 | 대한광통신 | 10 | 30.00 | 1.94 | -27.16 | -39.19 |
| 034020 | 두산에너빌리티 | 13 | 23.08 | 1.27 | -34.87 | -35.59 |
| 089030 | 테크윙 | 8 | 37.50 | 16.16 | 276.60 | -10.31 |
| 090430 | 아모레퍼시픽 | 6 | 16.67 | 1.34 | -7.60 | -10.14 |
| 329180 | HD현대중공업 | 13 | 30.77 | 2.66 | -3.42 | -46.35 |
| 267260 | HD현대일렉트릭 | 15 | 53.33 | 6.76 | 609.25 | -9.53 |
| **전체** | **전체 합산(균등비중)** | **102** | **33.33** | **5.79** | **128.98** | **-7.30** |

파일: `data/backtest_results_holdings.csv`, `data/backtest_trades_holdings.csv`

(2026-08-19 기준 아모레퍼시픽에 미청산 포지션 존재: 매수일 2026-07-31, 매수가 131,000원)

## 5. 발견 및 수정한 버그 — 전체 합산 수익률 계산 방식

처음에는 "전체 종목 합산"을 모든 종목의 매매를 매도일 순서로 그냥 복리로
이어붙이는 방식(`backtest.py`의 `compute_cumulative_return`을 통합 매매 내역에
그대로 적용)으로 계산했다. 보유종목 9개로 처음 돌렸을 때 이 방식으로 나온
총수익률이 **11,082%**라는 비현실적인 숫자였다.

원인: 이 방식은 "자본 100%가 매 신호마다 다음 거래로 즉시 옮겨간다"고 가정하는
셈인데, 실제로는 서로 다른 종목이 같은 기간에 동시에 포지션을 보유하는 경우가
많았다(102건 중 129쌍이 보유기간 겹침). 겹치는 기간만큼 자본이 이중 삼중으로
잡혀 수익률이 기하급수적으로 부풀려진 것이다.

수정: `backtest_multi.py`에 `build_equity_series()`/`combine_equal_weight_portfolio()`를
추가해서, 종목마다 자본을 균등 비중(1/N)으로 나눠 투자했다고 가정한 포트폴리오
누적자산 곡선을 만들고, 그 곡선의 최종 수익률과 최대낙폭(MDD)을 쓰도록 바꿨다.
- 8종목: 30.55% → **21.86%** (MDD -75.90% → -14.21%)
- 보유종목 9개: 11,082% → **128.98%** (MDD -68.71% → -7.30%)

승률/손익비는 매매 하나하나를 동등하게 세는 통계라 겹침과 무관해서 그대로 유효했다
(수정 전후 값 동일).

## 6. 이번 세션에서 만들거나 바꾼 파일

- `get_data.py` — 다종목 지원(`STOCKS`/`HOLDINGS` 딕셔너리, `--source`/`--code`/
  `--all`/`--holdings` 옵션), 키움 ka10081 연동 함수 추가
- `fibo_indicator.py` — `add_ema()` → `add_ma()`(SMA로 교체)
- `ichimoku_custom_indicator.py` — `FACTOR` 1.1→1, 시프트 제거, `add_senkou_spans()` 추가
- `backtest.py` — `fi.add_ema(df)` → `fi.add_ma(df)` 호출부 수정
- `backtest_multi.py` — 신규. 양운+피보크로스 다종목 백테스트, 균등비중 포트폴리오 집계
- `data/samsung.csv` 외 8종목 가격 데이터(`data/price_*.csv`)
- `data/backtest_results_multi.csv`, `data/backtest_trades_multi.csv`
- `data/backtest_results_holdings.csv`, `data/backtest_trades_holdings.csv`

## 7. 2026-08-20 오후 업데이트

### 7-1. 키움 REST API 단말기 인증(8050) 문제 원인 및 해결

- 원인: IP 등록 현황에 사무실 PC IP(58.72.22.38)만 등록돼 있었고, 지금 접속 중인
  노트북 IP(49.172.122.118)는 미등록이라 거부되고 있었다.
- 즉 에러 메시지의 "지정단말기 인증 실패"[8050]는 문구만 보고 짐작했던 별도의
  "단말기 등록" 개념이 아니라, 사실상 "미등록 IP에서의 접속 거부" 에러였다.
  1번 섹션에서 "IP 제한을 해제했는데도 동일 에러가 재현됐다"고 적었던 건, 그때
  해제/등록한 IP가 실제 접속 IP(노트북)와 달랐기 때문으로 보인다.
- 해결: 키움 개발자센터 > 계좌 App Key 관리 > IP 등록 및 현황에서 노트북 IP
  (49.172.122.118)를 추가 등록.
- 등록 과정에서 연결된 계좌가 2개(5225-4483 기존, 5384-7278 신규) 확인됐다.
  IP 등록은 계좌 단위가 아니라 앱(App Key) 단위로 공통 적용된다.

### 7-2. `get_data.py --source kiwoom` 정상 작동 확인

- IP 등록 후 토큰 발급이 정상적으로 되고, ka10081 페이지네이션(`cont-yn`/`next-key`)도
  의도대로 동작했다(삼성전자 기준 3페이지 이어받아 1220행 수집).
- 1번 섹션에서 응답 구조를 정확히 몰라 방어적으로 짜뒀던 부분
  (`open_pric`/`high_pric`/`low_pric`/`cur_prc`/`trde_qty` 필드명, "dt" 필드를
  가진 리스트를 찾는 탐색 로직)이 실제 응답과 맞아떨어짐을 확인했다.

### 7-3. FDR vs 키움 원본 데이터 비교 결과

같은 전략(양운+피보 GC/DC)으로 8종목/보유종목 9종목을 키움 원본으로 다시 받아
백테스트를 재실행하고, FDR 기준 결과(4번 섹션)와 비교했다.

- **8종목 그룹**: 백테스트 결과표가 FDR과 완전히 동일했다(파일 diff 없음).
- **보유종목 9종목 그룹**: 7종목은 완전히 동일했고, 에이피알/대한광통신 2종목만
  미세하게 달랐다(둘 다 수익률이 소폭 개선되는 방향). 매매건수·승률은 9종목
  전부 동일 — 같은 날 같은 신호가 떴고, 두 종목의 캔들 값이 아주 조금 달라서
  그 매매들의 손익폭만 살짝 바뀐 정도다.
  - 전체 합산 총수익률: 128.98% → **130.37%**
  - 전체 합산 손익비: 5.79 → **5.86**
  - 전체 합산 MDD: -7.30% → **-7.26%**
- **결론**: 애초 우려했던 "FACTOR=1.1 벤더보정" 문제는, 지표 공식 자체를
  정확하게 고치고 난 뒤(2번 섹션: FACTOR 1.1→1, MA를 EMA→SMA로 수정 등)에는
  이 백테스트 범위에서 사실상 무의미한 수준이었다. 즉 예전에 그 보정치가
  필요했던 건 FDR 데이터 자체의 문제라기보다, 예전 지표 공식(EMA 기반 MA,
  FACTOR 1.1)이 부정확했기 때문일 가능성이 크다. 그래도 데이터는 같은 소스
  (키움) 원본을 쓰는 쪽이 근본적으로 더 안전하므로, 앞으로는 키움 원본을
  기본 소스로 쓴다(`get_data.py`의 `--source` 기본값도 이미 `kiwoom`).

### 7-4. 최종 커밋 상태

- `9617c8c` — 키움 원본 기준 데이터(8종목+보유종목 9종목) 및 백테스트 결과
  재계산 반영, `origin/main`에 푸시 완료.

## 8. 다음에 이어서 할 일

- ~~키움 개발자센터에서 "지정단말기 인증" 관련 설정 재확인~~ → 7-1에서 해결됨
  (원인은 노트북 IP 미등록)
- (선택) `backtest_multi.py`의 `run_cloud_strategy()`에도 `backtest.py`처럼
  ADX 등 추가 필터를 넣어볼지 검토
- (선택) `get_data.py --source kiwoom --all/--holdings`로 가격 데이터를 정기
  갱신하는 걸 자동화(작업 스케줄러 등록)할지 검토
