# 세션 정리 — 지표 정밀검증 및 전략 확정 (2026-08-19 ~ 20)

## 1. 키움 REST API 직접 연동 시도 (get_data.py)

- `get_data.py`에 키움 REST API(`ka10081`, 주식일봉차트조회요청) 호출 기능을 추가했다.
  토큰 발급은 `get_kiwoom_ranking.py`의 `load_app_credentials()`/`issue_access_token()`을
  재사용하고, `cont-yn`/`next-key` 페이지네이션으로 5년치 일봉을 모은다.
- 목적: FinanceDataReader와 영웅문(키움) 간 데이터 벤더 차이 때문에 뒀던 보정값
  (`FACTOR=1.1`, 아래 2번 참고)을 근본적으로 없애는 것.
- **현재 상태: 키움 API가 막혀 있다.** 토큰 발급 시 `return_code: 3`,
  `"인증에 실패했습니다[8050:지정단말기 인증에 실패했습니다]"` 에러가 계속 발생한다.
  IP 제한을 해제해봤지만 동일 에러가 재현됐다 — IP 화이트리스트가 아니라 "지정단말기
  인증"이라는 별도 항목(키움 개발자센터의 단말기 등록/보안 옵션)이 원인으로 보이며,
  아직 해결되지 않았다.
- 그래서 이번 세션의 모든 데이터 수집은 `python get_data.py --source fdr ...`로,
  즉 기존 FinanceDataReader 경로로 진행했다. `--source kiwoom` 경로는 코드상으로는
  완성돼 있으니 키움 쪽 인증 문제가 풀리면 바로 시도해보면 된다.

## 2. 지표 공식 정밀검증 및 수정

### 2-1. `fibo_indicator.py` — MA5/MA8/MA13을 EMA → 단순이동평균(SMA)으로 변경

- 기존에는 `add_ema()`가 `Close.ewm(span=n, adjust=False)`로 지수이동평균(EMA)을
  계산했다. 이번에 요청받은 정확한 공식은 "단순이평(SMA), 종가기준"이라, 함수를
  `add_ma()`로 이름을 바꾸고 `Close.rolling(window=n).mean()`으로 교체했다.
- 피보4 = (MA5+MA8)/2, 피보5 = (MA8+MA13)/2 공식 자체는 그대로다 — 안에 들어가는
  MA의 종류만 EMA에서 SMA로 바뀌었다.
- 호출부인 `backtest.py`의 `fi.add_ema(df)`도 `fi.add_ma(df)`로 함께 수정했다
  (이 프로젝트에서 `add_ema`를 부르는 곳은 여기 한 곳뿐이었다).
- **주의**: 이 변경으로 삼성전자 단일종목 백테스트(`backtest.py`)의 골든/데드크로스
  타이밍과 매매 결과도 함께 바뀐다 — MA 계산 방식이 바뀌었으니 당연한 결과다.

### 2-2. `ichimoku_custom_indicator.py` — 엔상/엔하 FACTOR 1.1 → 1, 시프트 제거

- 이전 세션에서 영웅문 값과 맞춰보려고 역산해 넣었던 보정계수 `FACTOR=1.1`을,
  이번에 요청받은 원본 공식 그대로 `FACTOR=1`로 되돌렸다.
- 라인 공식(`LINE_SPECS`: 2선=AVG(ATR,10)×Factor, 3선=AVG(ATR,20)×Factor×2,
  4선=×3, 5선=×4)은 이미 정확히 일치해서 손대지 않았다.
- 항상 0이던 시프트 파라미터 `AA`(및 `.shift(AA)` 호출)를 제거했다 — 요청받은
  공식에 시프트 항목이 없어서, 있으나 마나 하던 코드를 걷어냈다.
- 참고: 엔상/엔하(ATR 인벨로프)는 이번에 확정한 백테스트 전략(아래 3번)에는
  쓰이지 않는다. 차트 시각화(`data/samsung_ichimoku_custom.png`)용 참고 지표다.

### 2-3. `ichimoku_custom_indicator.py` — 선1/선2(일목 선행스팬, 현재위치) 신규 추가

새 함수 `add_senkou_spans()`:

- 선1 = (HH10 + LL10 + HH25 + LL25) / 4
- 선2 = (HH50 + LL50) / 2
  (HHn/LLn = 최근 n일 고가 최댓값 / 저가 최솟값)
- 미래로 미리 그리는 시프트 없이(현재 위치 그대로) 계산한다.
- 양운/음운 판정(`구름상태` 컬럼): 선1 > 선2 → 양운, 선1 < 선2 → 음운.
  백테스트 전략에서는 문자열 컬럼 대신 `선1`/`선2` 값을 직접 비교해서 쓴다
  (부동소수점 동률 등 문자열 파싱 이슈를 피하기 위함).

## 3. 전략 확정 — "양운 + 피보 크로스"

`backtest_multi.py`에 구현. `backtest.py`가 ADX를 매수 필터로 쓰던 자리에
양운 여부를 넣은 구조다.

- **매수**: 피보 골든크로스(피보 구름 중심이 역피보 구름 중심을 아래→위로 돌파)가
  뜬 날, 그날 선1 > 선2(양운)이면 **다음 거래일 시가**에 매수. 양운이 아니면
  골든크로스가 떠도 매수하지 않는다.
- **매도**: 피보 데드크로스가 뜨면 양운/음운과 무관하게 무조건 **다음 거래일 시가**에
  매도.
- 종목당 동시에 1포지션만 보유(이미 보유 중이면 골든크로스 무시, 미보유 중이면
  데드크로스 무시).
- 매매 수수료(0.015%)/거래세(0.18%)는 `backtest.py`의 `BUY_FEE_RATE`/`SELL_FEE_RATE`를
  그대로 재사용.

## 4. 다종목 백테스트 결과

### 4-1. 시가총액 상위 + 업종 다양화 8종목 (`--group stocks`)

| 종목코드 | 종목명 | 매매건수 | 승률(%) | 손익비 | 총수익률(%) | MDD(%) |
|---|---|---|---|---|---|---|
| 005930 | 삼성전자 | 11 | 36.36 | 8.17 | 138.97 | -14.98 |
| 000660 | SK하이닉스 | 16 | 31.25 | 5.80 | 125.80 | -30.14 |
| 035420 | NAVER | 6 | 50.00 | 1.11 | 0.39 | -12.17 |
| 035720 | 카카오 | 8 | 12.50 | 0.75 | -41.53 | -39.27 |
| 373220 | LG에너지솔루션 | 8 | 25.00 | 0.98 | -34.33 | -42.69 |
| 005380 | 현대차 | 13 | 30.77 | 5.59 | 48.41 | -12.88 |
| 105560 | KB금융 | 20 | 30.00 | 0.51 | -53.24 | -49.10 |
| 207940 | 삼성바이오로직스 | 11 | 18.18 | 3.52 | -9.55 | -13.53 |
| **전체** | **전체 합산(균등비중)** | **93** | **29.03** | **3.33** | **21.86** | **-14.21** |

파일: `data/backtest_results_multi.csv`, `data/backtest_trades_multi.csv`

### 4-2. 실제 보유종목 9종목 (`--group holdings`)

| 종목코드 | 종목명 | 매매건수 | 승률(%) | 손익비 | 총수익률(%) | MDD(%) |
|---|---|---|---|---|---|---|
| 005930 | 삼성전자 | 11 | 36.36 | 8.17 | 138.97 | -14.98 |
| 000660 | SK하이닉스 | 16 | 31.25 | 5.80 | 125.80 | -30.14 |
| 278470 | 에이피알 | 10 | 30.00 | 7.69 | 83.27 | -18.72 |
| 010170 | 대한광통신 | 10 | 30.00 | 1.94 | -27.16 | -39.19 |
| 034020 | 두산에너빌리티 | 13 | 23.08 | 1.27 | -34.87 | -35.59 |
| 089030 | 테크윙 | 8 | 37.50 | 16.16 | 276.60 | -10.31 |
| 090430 | 아모레퍼시픽 | 6 | 16.67 | 1.34 | -7.60 | -10.14 |
| 329180 | HD현대중공업 | 13 | 30.77 | 2.66 | -3.42 | -46.35 |
| 267260 | HD현대일렉트릭 | 15 | 53.33 | 6.76 | 609.25 | -9.53 |
| **전체** | **전체 합산(균등비중)** | **102** | **33.33** | **5.79** | **128.98** | **-7.30** |

파일: `data/backtest_results_holdings.csv`, `data/backtest_trades_holdings.csv`

(2026-08-19 기준 아모레퍼시픽에 미청산 포지션 존재: 매수일 2026-07-31, 매수가 131,000원)

## 5. 발견 및 수정한 버그 — 전체 합산 수익률 계산 방식

처음에는 "전체 종목 합산"을 모든 종목의 매매를 매도일 순서로 그냥 복리로
이어붙이는 방식(`backtest.py`의 `compute_cumulative_return`을 통합 매매 내역에
그대로 적용)으로 계산했다. 보유종목 9개로 처음 돌렸을 때 이 방식으로 나온
총수익률이 **11,082%**라는 비현실적인 숫자였다.

원인: 이 방식은 "자본 100%가 매 신호마다 다음 거래로 즉시 옮겨간다"고 가정하는
셈인데, 실제로는 서로 다른 종목이 같은 기간에 동시에 포지션을 보유하는 경우가
많았다(102건 중 129쌍이 보유기간 겹침). 겹치는 기간만큼 자본이 이중 삼중으로
잡혀 수익률이 기하급수적으로 부풀려진 것이다.

수정: `backtest_multi.py`에 `build_equity_series()`/`combine_equal_weight_portfolio()`를
추가해서, 종목마다 자본을 균등 비중(1/N)으로 나눠 투자했다고 가정한 포트폴리오
누적자산 곡선을 만들고, 그 곡선의 최종 수익률과 최대낙폭(MDD)을 쓰도록 바꿨다.
- 8종목: 30.55% → **21.86%** (MDD -75.90% → -14.21%)
- 보유종목 9개: 11,082% → **128.98%** (MDD -68.71% → -7.30%)

승률/손익비는 매매 하나하나를 동등하게 세는 통계라 겹침과 무관해서 그대로 유효했다
(수정 전후 값 동일).

## 6. 이번 세션에서 만들거나 바꾼 파일

- `get_data.py` — 다종목 지원(`STOCKS`/`HOLDINGS` 딕셔너리, `--source`/`--code`/
  `--all`/`--holdings` 옵션), 키움 ka10081 연동 함수 추가
- `fibo_indicator.py` — `add_ema()` → `add_ma()`(SMA로 교체)
- `ichimoku_custom_indicator.py` — `FACTOR` 1.1→1, 시프트 제거, `add_senkou_spans()` 추가
- `backtest.py` — `fi.add_ema(df)` → `fi.add_ma(df)` 호출부 수정
- `backtest_multi.py` — 신규. 양운+피보크로스 다종목 백테스트, 균등비중 포트폴리오 집계
- `data/samsung.csv` 외 8종목 가격 데이터(`data/price_*.csv`)
- `data/backtest_results_multi.csv`, `data/backtest_trades_multi.csv`
- `data/backtest_results_holdings.csv`, `data/backtest_trades_holdings.csv`

## 7. 2026-08-20 오후 업데이트

### 7-1. 키움 REST API 단말기 인증(8050) 문제 원인 및 해결

- 원인: IP 등록 현황에 사무실 PC IP(58.72.22.38)만 등록돼 있었고, 지금 접속 중인
  노트북 IP(49.172.122.118)는 미등록이라 거부되고 있었다.
- 즉 에러 메시지의 "지정단말기 인증 실패"[8050]는 문구만 보고 짐작했던 별도의
  "단말기 등록" 개념이 아니라, 사실상 "미등록 IP에서의 접속 거부" 에러였다.
  1번 섹션에서 "IP 제한을 해제했는데도 동일 에러가 재현됐다"고 적었던 건, 그때
  해제/등록한 IP가 실제 접속 IP(노트북)와 달랐기 때문으로 보인다.
- 해결: 키움 개발자센터 > 계좌 App Key 관리 > IP 등록 및 현황에서 노트북 IP
  (49.172.122.118)를 추가 등록.
- 등록 과정에서 연결된 계좌가 2개(5225-4483 기존, 5384-7278 신규) 확인됐다.
  IP 등록은 계좌 단위가 아니라 앱(App Key) 단위로 공통 적용된다.

### 7-2. `get_data.py --source kiwoom` 정상 작동 확인

- IP 등록 후 토큰 발급이 정상적으로 되고, ka10081 페이지네이션(`cont-yn`/`next-key`)도
  의도대로 동작했다(삼성전자 기준 3페이지 이어받아 1220행 수집).
- 1번 섹션에서 응답 구조를 정확히 몰라 방어적으로 짜뒀던 부분
  (`open_pric`/`high_pric`/`low_pric`/`cur_prc`/`trde_qty` 필드명, "dt" 필드를
  가진 리스트를 찾는 탐색 로직)이 실제 응답과 맞아떨어짐을 확인했다.

### 7-3. FDR vs 키움 원본 데이터 비교 결과

같은 전략(양운+피보 GC/DC)으로 8종목/보유종목 9종목을 키움 원본으로 다시 받아
백테스트를 재실행하고, FDR 기준 결과(4번 섹션)와 비교했다.

- **8종목 그룹**: 백테스트 결과표가 FDR과 완전히 동일했다(파일 diff 없음).
- **보유종목 9종목 그룹**: 7종목은 완전히 동일했고, 에이피알/대한광통신 2종목만
  미세하게 달랐다(둘 다 수익률이 소폭 개선되는 방향). 매매건수·승률은 9종목
  전부 동일 — 같은 날 같은 신호가 떴고, 두 종목의 캔들 값이 아주 조금 달라서
  그 매매들의 손익폭만 살짝 바뀐 정도다.
  - 전체 합산 총수익률: 128.98% → **130.37%**
  - 전체 합산 손익비: 5.79 → **5.86**
  - 전체 합산 MDD: -7.30% → **-7.26%**
- **결론**: 애초 우려했던 "FACTOR=1.1 벤더보정" 문제는, 지표 공식 자체를
  정확하게 고치고 난 뒤(2번 섹션: FACTOR 1.1→1, MA를 EMA→SMA로 수정 등)에는
  이 백테스트 범위에서 사실상 무의미한 수준이었다. 즉 예전에 그 보정치가
  필요했던 건 FDR 데이터 자체의 문제라기보다, 예전 지표 공식(EMA 기반 MA,
  FACTOR 1.1)이 부정확했기 때문일 가능성이 크다. 그래도 데이터는 같은 소스
  (키움) 원본을 쓰는 쪽이 근본적으로 더 안전하므로, 앞으로는 키움 원본을
  기본 소스로 쓴다(`get_data.py`의 `--source` 기본값도 이미 `kiwoom`).

### 7-4. 최종 커밋 상태

- `9617c8c` — 키움 원본 기준 데이터(8종목+보유종목 9종목) 및 백테스트 결과
  재계산 반영, `origin/main`에 푸시 완료.

## 8. 2026-08-19 세션 다음에 이어서 할 일 (아래 9번에서 진행됨)

- ~~키움 개발자센터에서 "지정단말기 인증" 관련 설정 재확인~~ → 7-1에서 해결됨
  (원인은 노트북 IP 미등록)
- (선택) `backtest_multi.py`의 `run_cloud_strategy()`에도 `backtest.py`처럼
  ADX 등 추가 필터를 넣어볼지 검토
- (선택) `get_data.py --source kiwoom --all/--holdings`로 가격 데이터를 정기
  갱신하는 걸 자동화(작업 스케줄러 등록)할지 검토

---

# 9. 2026-08-20 세션 — SOXL 백테스트, MDD 계산 방식 교정, "43/-43+Up/Down 예약매수" 전략 확정

## 9-1. SOXL(미국 3배 레버리지 반도체 ETF) 백테스트 인프라 추가

- `get_data.py`: `--source fdr`에서 미국 티커(야후 파이낸스 백엔드)가 국내종목과
  다른 형식(인덱스명 없음, `Change` 대신 `Adj Close`)으로 오는 걸 발견해 정규화
  (`fetch_fdr_ohlcv()`가 `Date` 인덱스명과 `Change` 컬럼을 항상 채우도록 수정).
  기본 5년 lookback 대신 임의 시작일을 쓸 수 있는 `--start` 옵션도 추가(다른 종목
  동작에는 영향 없음).
- `data/price_SOXL.csv`: SOXL 상장일(2010-03-11)부터 전체 기간(4,136거래일) 수집.
  `python get_data.py --source fdr --code SOXL --start 2010-03-11`로 재현 가능.
- `backtest_multi.py`에 `"soxl"` 그룹 추가(기존 양운+피보크로스 완전진입/청산 전략
  그대로 SOXL에 적용) → `data/backtest_results_soxl.csv`. 결과: 총수익률 +96.32%,
  MDD -57.85%(구 계산방식) — Buy&Hold(+18,689%)에는 크게 못 미치지만 MDD는
  Buy&Hold(-90.51%)보다 훨씬 낮았다.

## 9-2. "부분 비중 유지" 아이디어 — 완전청산 대신 음운에도 일부 비중 유지

- `backtest_soxl_partial.py`(신규): 피보 GC/DC 트리거는 그대로 두되, DC가 떠도
  0%로 전량청산하지 않고 목표비중(0/30/50%)까지만 줄이고 다음 GC(+양운 필터)에서
  다시 100%로 채우는 일별 mark-to-market 시뮬레이션. 목표비중=0%는 기존 완전청산과
  수학적으로 동일함을 확인(검증됨).
- 결과(SOXL 전체기간): 완전청산 +96.29%(MDD -65.24%) → 음운30%유지 **+2,076.18%**
  (MDD -58.27%) → 음운50%유지 +6,419.56%(MDD -69.84%). 완전청산이 휩쏘 손실을
  누적시켜 오히려 손해라는 게 핵심 발견.
- 같은 아이디어를 국내 부진종목 4개(대한광통신/두산에너빌리티/아모레퍼시픽/
  HD현대중공업)에도 적용(`backtest_partial_weight_holdings.py` →
  `data/backtest_results_holdings_partial.csv`). 대한광통신 -27%→+61%,
  두산에너빌리티 -35%→+19%, HD현대중공업 -3%→+64% 등 대부분 개선.

## 9-3. MDD 계산 방식을 "거래단위 복리" → "일별 mark-to-market"으로 교체

- `metrics.py`의 `calc_mdd(trades, price_df)`가 이제 `build_daily_equity()`로
  매수일~매도일 사이 일별 시가/종가 변동을 그대로 반영한 자산곡선의 낙폭을 계산한다
  (예전엔 완료된 거래 결과값만 순서대로 복리 계산해서, 거래 도중의 장중 고점/저점을
  놓쳤다 - 변동성 큰 구간일수록 MDD를 과소평가하는 문제가 있었다).
- `backtest_multi.py`의 `summarize()`도 종목별 가격 데이터를 넘겨받아 이 방식을 쓰도록
  수정(`data/backtest_results_multi.csv`/`data/backtest_results_holdings.csv` 재계산).
- **가설(국내종목은 차이가 작을 것) 기각**: 거의 모든 종목이 4~21%p 더 깊어졌고
  (삼성전자 -14.98%→-31.98%, 테크윙 -10.31%→-31.69%), 원인은 2025~2026년 국내
  반도체/AI 관련주도 SOXL 못지않은 급등락을 겪었기 때문. 레버리지 여부와 무관하게
  변동성 큰 구간이면 나타나는, 계산 방식 자체의 맹점이었다.
- **주의**: "전체 합산(균등비중)" 행의 MDD는 아직 예전 방식(`build_equity_series`)을
  쓴다 - 여기까지는 안 바꿨음. 다음에 필요하면 통일할 것.
- 재계산 전 백업: `data/backtest_results_multi_OLD.csv`, `data/backtest_results_holdings_OLD.csv`.

## 9-4. "43/-43 + Up/Down 구름 예약매수" — 실전 매매 방식 정밀 구현

새 지표 모듈 `up_down_43_indicator.py`, 새 백테스트 `backtest_soxl_reserved.py`.
공식과 파라미터는 여러 차례 확인을 거쳐 사용자가 직접 확정했다 — **아래 9-6절이
최종 확정 스펙이니, 다음 세션에서 실전 적용 검토 시 그것만 보면 된다.**

지표 공식(전체):

```
Up/Down 일목 (P1=25, P2=50, D1=2)
  BBandsUp/Down(P,D) = MA(Close,P) ± D×STD(Close,P)   (STD는 ddof=0 모집단표준편차)
  Up4   = (lowest(Low,P1)  + BBandsDown(P1,D1)) / 2
  Up5   = (lowest(Low,P2)  + BBandsDown(P2,D1)) / 2
  Down4 = (highest(High,P1)+ BBandsUp(P1,D1))   / 2
  Down5 = (highest(High,P2)+ BBandsUp(P2,D1))   / 2
  Up구름 = Up4~Up5 사이 밴드(지지), Down구름 = Down4~Down5 사이 밴드(저항)

43/-43 일목 (P1=50, P2=25) — C/43의4·5/-43의4·5는 참고용, 신호에는 미사용
  C       = (HH(P1)+LL(P1)+MA(P1)+MA(P2)) / 4                (shift 없음)
  43의4   = (2·HH(P1)+LL(P1)) / 3,   43의5   = (3·HH(P1)+LL(P1)) / 4
  -43의4  = (HH(P1)+2·LL(P1)) / 3,   -43의5  = (HH(P1)+3·LL(P1)) / 4
  S = HH(P1) + (HH(P1)-LL(P1))/4,   G = LL(P1) - (HH(P1)-LL(P1))/4
  S,G 각각 .shift(15) 적용(15일 선행shift - 미래참조 아님, 계산은 당일 데이터로 함)
```

원래 매매규칙(분할 2단계, 실전 방식):
- 1차 매수: 종가가 Up구름 하단 아래로 찍었다가 다시 위로 올라오면(지지 후 반등)
- 2차 매수: 종가가 G라인 아래로 이탈했다가 다시 위로 회복하면
- 1차 매도: 종가가 Down구름 위로 돌파하면
- 2차 매도: 종가가 S라인 위로 돌파했다가 다시 아래로 내려오면
- 포지션 크기는 "각 20~30주, 최대 60~70주" 실전 규칙을 **다리(leg)당 최대비중의
  1/3**로 근사(3다리=100%) — SOXL이 16년간 200배 넘게 올라 절대 주식수를
  전체 구간에 그대로 못 써서 비중(%)으로 환산했다.

기본형 결과(전체기간): 총수익률 +129.43%, MDD **-86.09%**(2022-02-09 고점→
2022-10-14 저점, Buy&Hold MDD 저점과 정확히 같은 날) - 매도조건 2개가 전부
"위로 돌파"해야 발동해서, 반등 없이 계속 밀리는 하락장엔 손절 수단이 없다는 게
핵심 리스크로 확인됨.

## 9-5. 하방 방어 시도 4가지 비교

| 방어 방식 | 전체 총수익률 | 전체 MDD | 2022 총수익률 | 2022 MDD | 채택여부 |
|---|---|---|---|---|---|
| (기준) 방어 없음 | +129.43% | -86.09% | -77.08% | -86.09% | - |
| 선1/선2 전환 시 전량청산 | +44.49% | -82.74% | -66.58% | -76.72% | 기각 - 전환 순간에만 팔고 그 뒤 음운 중에도 눌림목매수가 그대로 재진입해 방어 효과 미미, 수익률만 악화 |
| 위 + 음운엔 매수 자체 차단 | 0%(매매 0건) | - | - | - | 기각 - "Up구름 반등" 매수신호 35건 전부가 음운 상태에서 발생하는 구조라, 필터를 걸면 매수 자체가 사라짐 |
| **G라인 역전 시 매수신호 무시** | +92.76% | -78.70% | -64.90% | -78.70% | 기각(차선) - 방어는 되나 수익률 개선폭이 작음 |
| **G라인 역전 시 비중 1/3 축소** | **+323.28%** | **-78.68%** | **-33.17%** | **-50.64%** | **채택** |

**G라인 역전**(`analyze_g_inversion_event.py`로 발견/검증): G가 Up구름 상단 위로
올라서는 상태(`G > max(Up4,Up5)`)를 이벤트로 잡아 SOXL/삼성전자 이벤트 스터디를
해본 결과, SOXL은 23건의 이벤트 이후 3일 평균변동성이 9.96%(전체 기준선 5.77%
대비 +4.19%p)로 뚜렷이 높았고, 이벤트 날짜에 2020-03-09(코로나 폭락 초입),
2022년 하락장 구간(01-31/05-02/05-12/07-05/10-05/10-10, 23건 중 6건)이 포함돼
"변동성 급등 예고 신호"라는 가설이 검증됐다. 삼성전자는 이벤트가 7건뿐이라 효과가
약함 - SOXL/고변동성 자산 전용 신호로 보인다.
(`data/g_inversion_events_soxl.csv`, `data/g_inversion_events_samsung.csv`)

## 9-6. ATR 트레일링스탑 결합 — ★ 최종 확정 전략 스펙 ★

ATR(20일, True Range 단순평균 - Wilder 지수평활 아님)을 추가해 "매수 이후 종가
기준 최고점 대비 ATR20×배수만큼 빠지면 전량 손절"을 결합했다. 배수 2/3/4 비교
결과 **3배가 스위트스팟**(2배는 너무 타이트해서 정상 변동에도 자주 손절당해
전체 수익률이 마이너스로 망가짐, 4배는 2022년 방어력이 3배보다 떨어짐).

**최종 확정 전략 (다음 세션 실전 적용 검토 대상)**:

```
[포지션 단위] 다리(leg)당 최대비중의 1/3, 최대 100%(=실전 "20~30주/60~70주" 근사)

[매수 - 각각 발동 시 비중 +1/3, 최대 100%에서 중단]
1차 매수: 전일 종가 < Up구름 하단  AND  당일 종가 >= Up구름 하단  (지지 후 반등)
2차 매수: 전일 종가 < G라인        AND  당일 종가 >= G라인        (이탈 후 회복)

[매도 - 각각 발동 시 비중 -1/3, 0%에서 중단]
1차 매도: 전일 종가 < Down구름 상단  AND  당일 종가 >= Down구름 상단  (상향 돌파)
2차 매도: 전일 종가 > S라인          AND  당일 종가 <= S라인          (돌파 후 재하락)

[리스크관리 - 우선순위 1위, 위 매수/매도 신호보다 항상 먼저 적용]
ATR 트레일링스탑: 보유 중(비중>0) 매수 이후 종가 기준 최고점을 매일 갱신하다가,
  당일 종가 <= (최고종가 - ATR20 × 3) 이면 → 다음 거래일 시가에 비중 전량(100%) 손절

[리스크관리 - 우선순위 2위]
G라인 역전 감지: G가 Up구름 상단 위로 "처음" 올라서는 날 → 다음 거래일 시가에
  그날 평소 신호와 별개로 비중 1/3 추가 축소(다른 매수/매도 신호와 합산되어 계산됨)

[체결 규칙]
모든 신호는 당일 종가로 확정, 다음 거래일 시가에 체결.
매수 수수료 0.015%, 매도 수수료 0.015%+거래세 0.18%(기존 BUY_FEE_RATE/SELL_FEE_RATE).

[검토했지만 최종안에 포함하지 않은 것]
- 선1/선2(양운/음운) 기반 강제청산·매수차단: 위 9-5 표에서 기각됨
- G역전 시 "매수신호 무시": 채택된 "비중 1/3 축소"보다 성과가 낮아 기각
```

**최종 성과 (SOXL 2010-03-11~2026-08-19, mark-to-market)**:

| 구간 | 총수익률 | MDD | 참고 |
|---|---|---|---|
| 전체기간 | **+358.99%** | **-57.53%** | CAGR 9.71%, Calmar 0.17, Buy&Hold(+18,689%) 대비 포착률 1.92% |
| 2022년 하락장만 | **-2.18%** | **-32.77%** | 기존(방어 없음) 대비 -77.08%/-86.09%에서 크게 개선 - 사실상 방어 성공 |
| 2024-05-01~2026-08-19 | **+120.18%** | -51.56% | 승률 62.5%, 손익비 7.19, 매도체결 8건 |

파일: `data/backtest_soxl_final_comparison.csv`(6개 변형 × 전체/2022 비교),
`data/backtest_trades_soxl_reserved*.csv`(변형별 체결내역).

## 9-7. 미해결 - 다음 세션에서 확인할 것

- **실전 성과(2024-05-01~ 구간 $5,000→$14,000, +180%)와 백테스트 격차**: "방어
  없음" 기본형은 이 구간 백테스트가 +4.84%(MDD -80.23%)로 실전과 크게 어긋났다.
  최종 확정(G역전+ATRx3) 버전으로 다시 계산하면 +120.18%로 훨씬 가까워지긴
  했지만(9-6 표 참고) 여전히 +180%에는 못 미친다. 실제 매매기록과 대조해서
  어느 신호가 실제 타이밍과 다르게 판정됐는지 확인이 필요하다.
- 포지션 사이징이 절대 주식수(20~30주/60~70주)가 아니라 비중(%) 근사치라는 점 -
  실전 재적용 시 현재 계좌 규모에 맞춰 다시 환산해야 한다.
- G라인 역전 신호는 국내종목(삼성전자)에는 뚜렷하지 않았다(이벤트 7건, 효과 약함) -
  이 리스크관리 조합 전체가 SOXL/고변동성 레버리지 자산 전용일 가능성이 있다.
- "전체 합산(균등비중)" MDD(9-3 참고)를 mark-to-market으로 통일할지 결정 필요.

## 9-8. 이번 세션에서 만들거나 바꾼 파일

- `get_data.py` — `--start` 옵션, 미국 티커 FDR 포맷 정규화
- `metrics.py` — `calc_mdd()`를 mark-to-market 방식으로 교체, `build_daily_equity()` 추가
- `backtest_multi.py` — `summarize()`가 가격데이터를 받아 mark-to-market MDD 계산, `"soxl"` 그룹 추가
- `backtest_soxl_partial.py`, `backtest_partial_weight_holdings.py` — 부분비중 유지 백테스트(신규)
- `up_down_43_indicator.py` — Up/Down구름 + 43/-43(S/G 포함) 지표 계산(신규)
- `backtest_soxl_reserved.py` — "43/-43+Up/Down 예약매수" 전략 + 방어 방식들 + ATR스탑 비교(신규)
- `analyze_g_inversion_event.py` — G라인 역전 이벤트 스터디(신규)
- `data/price_SOXL.csv` 및 위에서 언급된 `data/backtest_*soxl*.csv` 결과 파일들 일체

---

# 10. 2026-08-20 세션(이어서) — "추세필터+꼭지바닥 롤링매매" 전략 신규 개발 및 G역전1/3축소 최종 확정

9번 섹션의 "43/-43+Up/Down 예약매수"와는 별개로, 같은 세션에서 이어서 개발한 새 전략.
엔상/엔하(9-4 이전부터 있던 참고용 지표, `ichimoku_custom_indicator.py`)를 실제 매매
신호로 처음 사용했고, 신규 지표(꼭지4/바닥4)를 새로 만들었다.

## 10-1. 최종전략 스펙 ★

새 지표 모듈 `kkokji_badak_indicator.py`(신규), 새 백테스트 `backtest_soxl_trend_rolling.py`(신규).

```
[추세판정] MA17(엔상1선과 동일한 기준선, 엔상2/엔하2 밴드의 중심 - 엔하1선은 후행스팬이라 별개) 5일 기울기
  MA17 = MA(Close, 17),  기울기 = MA17.diff(5)
  기울기 > 0 → 상승추세,  기울기 < 0 → 하락추세

[지표 정의]
  엔하2 = MA17 - AVG(ATR(14), 10) × 1        (Period=17, ATRPeriod=14, Factor=1 - ichimoku_custom_indicator.py 기존 공식 그대로)
  엔상2 = MA17 + AVG(ATR(14), 10) × 1
  X     = (highest(High,4) + lowest(Low,4) + highest(High,8) + lowest(Low,8)) / 4   (shortPeriod=4, midPeriod=8)
  꼭지4 = (X + BBandsUp(Close,8,4))   / 2    (BBands는 MA±D1×STD, ddof=0, D1=4)
  바닥4 = (X + BBandsDown(Close,8,4)) / 2
  ※ longPeriod=16은 위 공식에 실제로 쓰이지 않음(향후 꼭지5/바닥5 대비 예약 파라미터)

[매수 - 상승추세일 때만, 최대 6유닛(유닛=최대비중의 1/6)]
  엔하2 터치(전일 종가<엔하2 AND 당일 종가>=엔하2, 이탈 후 회복) → 1유닛
  바닥4 터치(같은 방식)                                          → 2유닛

[매도 - 하락추세일 때만]
  엔상2 터치(전일 종가>엔상2 AND 당일 종가<=엔상2, 돌파 후 재하락) → 1유닛씩 부분매도
  꼭지4 터치(같은 방식)                                            → 잔량 전량매도(엔상2보다 우선)
  ※ 상승추세 유지 중에는 매도 조건 자체를 평가하지 않으므로, 꼭지4 터치가 떠도 홀드(매도 안 함)

[리스크관리 - G역전 1/3 비중축소] (9-6절과 같은 G역전 정의: G가 Up구름 상단 위로 역전)
  G역전 상태로 "처음" 진입하는 날 → 다음 거래일 시가에 그날 다른 신호와 별개로 비중 1/3 추가 축소
  G가 정상(Up구름 아래)으로 돌아와도 축소분을 자동 복구하지 않음 - 재진입은 엔하2/바닥4 터치로만

[체결 규칙] 신호는 당일 종가로 확정, 다음 거래일 시가에 체결(수수료: 기존 BUY_FEE_RATE/SELL_FEE_RATE 재사용)
```

**최종 성과 (SOXL 2010-03-11~2026-08-19, mark-to-market, 매도체결 36건)**:

| 지표 | 값 |
|---|---|
| 총수익률 | **+1,086.71%** |
| MDD | **-72.04%** |
| Calmar(총수익률/\|MDD\|) | **15.08** |

(2022년 하락장만 별도로 자른 성과는 이번 조합으로는 아직 계산하지 않음 - 10-4 참고)

## 10-2. 오늘 테스트했다가 기각한 조합들과 이유

**ATR(20) 트레일링스탑 단독 (배수 스윕 x3~x8)**: 9-6절 전략(재진입이 드묾)에는 x3가
스위트스팟이었지만, 이 전략은 상승추세 중 재진입이 훨씬 잦아서 상성이 다르게 나타났다.

| 배수 | 총수익률 | MDD | ATR스탑청산건수 | ATR스탑 평균수익률 |
|---|---|---|---|---|
| (기준, 스탑없음) | +1,821.44% | -80.38% | - | - |
| x3 | +29.39% | -32.56% | - | - |
| x4 | +15.76% | -47.12% | 25건 | **-5.01%** |
| x5 | +22.08% | -68.73% | 19건 | **-5.77%** |
| x6 | +230.66% | -70.98% | 15건 | **-5.20%** |
| x8 | +715.12% | -82.68% | 5건 | +25.24% |

x4~x6은 ATR스탑 발동 건의 평균수익률이 전부 -5%대 손실 → 대부분 진짜 추세반전이
아니라 상승추세 중 정상 눌림에 걸려 "손절→재매수"를 반복하는 whipsaw로 확인됨.
x8은 발동건수가 줄고 그 건들의 평균이 +25.24%(진짜 하락추세를 잡아냄)로 개선되지만,
MDD는 오히려 기준선보다 나빠짐(배수가 느슨해서 최악의 낙폭 구간을 못 막음). **결론:
이 전략에는 ATR 트레일링스탑을 어떤 배수로도 유의미하게 결합할 수 없다(기각)**.

**G역전1/3축소 + ATR×6스탑 결합**: 단독 최선이었던 ATR×6과 채택된 G역전1/3축소를
합쳐봤으나, MDD는 -72.04%→-69.58%로 2.5%p만 개선되고 총수익률은 1,086.71%→314.12%로
급감, Calmar는 15.08→4.51로 악화. **기각 - 두 방어장치가 중복되며 서로 도움이 안 됨**.

**"터치" 정의**: "즉시 터치(라인에 닿는 즉시 신호)" vs "터치 후 반대방향 확인(9-4의
Up구름/S라인 매매와 동일 패턴)" 두 방식을 검토했고, 후자(확인방식)로 결정해 구현·백테스트했다.
"즉시터치" 버전은 실제로 백테스트해서 정량 비교하지는 않았다(10-4 참고 - 미검증).

## 10-3. 기존 최종전략(9번, 4개신호+G역전+ATR×3)과의 관계

| | 9번 최종전략 | 10번 신규전략(G역전1/3축소) |
|---|---|---|
| 매수/매도 신호 | Up/Down구름 + S/G라인(43/-43 일목) | 엔상2/엔하2(ATR 인벨로프) + 꼭지4/바닥4(신규) |
| 추세필터 | 없음(신호 자체가 추세 내장) | MA17 5일 기울기로 상승/하락 구분 |
| 리스크관리 | ATR×3 트레일링스탑 + G역전1/3축소 | G역전1/3축소만 |
| 총수익률(SOXL 전체기간) | +358.99% | **+1,086.71%** |
| MDD | -57.53% | -72.04% |
| Calmar | 0.17(별도 계산) | 15.08 |

10번 신규전략이 총수익률에서 크게 앞서지만(약 3배), MDD는 9번보다 더 깊다(방어 수단이
G역전 하나뿐이라서). 둘 다 **SOXL 단일 종목으로만 검증됐다** - 9-7절에서 이미 "G역전
신호는 삼성전자엔 효과가 약함(이벤트 7건뿐)"이 확인된 상태라, 10번 전략도 다른 종목/자산
군에서 그대로 통할지는 아직 알 수 없다.

## 10-4. 다음 단계 후보

- 이 전략(10번)도 삼성전자 등 다른 종목으로 교차검증 필요 - 특히 엔상/엔하 ATR 인벨로프와
  꼭지/바닥 지표가 SOXL 특유의 변동성에 맞춰진 것인지, 아니면 범용적인지 확인.
- "즉시터치" 버전도 별도로 백테스트해서 "터치 후 확인" 방식과 정량 비교(10-2절 - 아직 안 함).
- 실전 신호생성기(`signal_generator.py`, 현재는 9번 전략 기준)를 이 새 전략(10번) 기준으로
  업데이트할지 검토.

## 10-5. 이번 세션에서 만들거나 바꾼 파일

- `kkokji_badak_indicator.py` — 꼭지4/바닥4 지표 계산(신규)
- `backtest_soxl_trend_rolling.py` — 추세필터+꼭지바닥 롤링매매 백테스트, ATR스탑/G역전
  변형 비교(신규)
- `data/backtest_soxl_trend_rolling.csv` — 신규전략 vs 9번 최종전략 기본 비교
- `data/backtest_soxl_trend_rolling_atrstop.csv` — ATR×3~6스탑 결합 비교(전체기간+2022)
- `data/backtest_soxl_trend_rolling_atrsweep.csv` — ATR×4/5/6/8 스윕(whipsaw 진단 포함)
- `data/backtest_soxl_trend_rolling_ginversion.csv` — G역전1/3축소 단독/+ATR×6 비교
